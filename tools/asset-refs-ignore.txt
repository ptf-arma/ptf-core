# Known-broken asset references, tolerated by tools/check_asset_refs.py so it
# can gate on NEW breakage without failing on this pre-existing tech debt.
# Burn these down as the art/paths get fixed, then delete the line.
#
# One reference per line, exactly as the checker prints it. Blank lines and
# lines starting with # are ignored.

# --- PTF_Helmets references assets that aren't in the repo (need the art):
\z\PTF\addons\PTF_Textures\gear\helmets\ptf_mich_covered_rhino_lowe.rvmat
