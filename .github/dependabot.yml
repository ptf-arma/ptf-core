version: 2
updates:
  # Keep the GitHub Actions we pin (checkout@v4, setup-python@v5, …) current
  # so we pick up fixes and don't drift onto deprecated runners.
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    commit-message:
      prefix: "ci"
    labels: ["build"]
