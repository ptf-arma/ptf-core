# HEMTT build output
/.hemttout
/releases

# Legacy SCons junction staging dir (safe to delete; not used anymore)
/build

# Signing keys (never commit private keys)
*.hemttprivatekey
*.biprivatekey
*.bisign

# Built PBOs and release mod folder
*.pbo
@\[PTF\] Paramarine Milsim Core/

# ctab is not built from source here; it ships as a prebuilt external PBO
# merged from the Workshop copy at release time (see BUILDING.md).
/addons/ctab/

# Python bytecode from the tests/ harness
__pycache__/
*.pyc

# PTF_Sound raw audio cache (TTS fetches, synthesized music) - regenerable,
# but kept out of temp so regeneration is deterministic across runs
/tools/.sound_cache/

# Local, uncommitted project notes
CLAUDE.local.md
