// X.Y.Z version. Only MAJOR (X) is hand-maintained here — bump it for a major
// overhaul. MINOR (Y = number of releases) and PATCH (Z = develop updates since
// the last release) are generated from git by tools/gen-version.sh (CI) and
// tools/release.ps1 (Workshop build) at build time; the committed values below
// are just a placeholder default.
#define MAJOR 1
#define MINOR 0
#define PATCH 324
